services:
  nginx:
    ports:
      - "80:80"
    restart: unless-stopped

  backend-app:
    env_file:
      - backend/.env.prod
    restart: unless-stopped

  frontend-app:
    env_file:
      - frontend/.env.prod
    restart: unless-stopped
